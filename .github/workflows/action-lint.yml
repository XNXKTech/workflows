name: Lint

on:
  pull_request:
    paths:
      - '**/*.yml'
  workflow_dispatch:

jobs:
  actionlint:
    name: Actions

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.1

      - uses: reviewdog/action-actionlint@v1
